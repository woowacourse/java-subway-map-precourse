# 프로그램 기능

### `초기 설정`

- [ ] 지하철역으로 교대역, 강남역, 역삼역, 남부터미널역, 양재역, 양재시민의숲역, 매봉역이 등록되어 있다.
- [ ] 지하철 노선으로 2호선, 3호선, 신분당선이 등록되어 있다.
- [ ] 노선에 역이 아래와 같이 등록되어 있다.(왼쪽 끝이 상행 종점)
    - 2호선: 교대역 - 강남역 - 역삼역
    - 3호선: 교대역 - 남부터미널역 - 양재역 - 매봉역
    - 신분당선: 강남역 - 양재역 - 양재시민의숲역

<br>

### `기능1` 메인 화면 <br>

- [ ] `1-a` 번호를 입력하면 해당 기능의 클래스로 이동
- [ ] `1-b` 종료 전까지 메인 화면을 반복
- [ ] `1-b` 1 ~ 4, Q 이외의 값 예외처리

<br>

### `기능2` 역 관리 <br>

`2-a` 역 관리 기능이 수행되면 메인으로, 에러인 경우 역관리로 돌아간다.

1. `2-b` 역 등록

    - [ ] 등록학 역 이름 입력 안내 출력
    - [ ] 등록할 역 이름 입력받기

        - [ ] [공통예외]한글 이외의 입력값 검사하기 👉 한글이 아니라면 예외처리
        - [ ] [공통예외]이름 2자 이상 검사하기 👉 이름이 2자 이상이 아니라면 예외처리
        - [ ] [역예외]역 이름 중복 검사 하기 👉 역이 존재한다면(=`역이중복됨`) 예외처리

    - [ ] 역 등록 : 완료 안내 출력

2. `2-c` 역 삭제

    - [ ] 삭제할 역 이름 입력 안내 출력
    - [ ] 삭제할 역 이름 입력받기

        - [ ] [공통예외]한글 이외의 입력값 검사하기 👉 한글이 아니라면 예외처리
        - [ ] [공통예외]이름 2자 이상인지 검사 👉 범위를 만족하지 못하면 예외처리
        - [ ] [역예외]역 중복 검사 👉 역이 존재하지 않는다면(=`역이중복되지않음`) 예외 처리
        - [ ] [노선예외2]노선에 역 중복 검사 👉 노선에 역이 존재한다면(=`노선에역이중복됨`) 예외 처리

          > "노선에 등록된 역은 삭제할 수 없다"

    - [ ] 역 삭제 : 삭제 완료 안내 출력하기

3. `2-d` 역 조회하기

    - [ ] 역 목록 출력하기

4. `2-e` 돌아가기(`B`)

<br>

### `기능3` 노선 관리 <br>

`3-a`. 노선 관리 기능이 제대로 수행되면 메인으로, 에러인 경우 노선 관리로 돌아간다.

1. `3-b` 노선 등록

    - [ ] 등록할 노선 입력 안내 출력
    - [ ] 등록할 노선 입력 받기

        - [ ] [공통예외]한글 이외의 입력값 검사 👉 한글이 아니라면 예외처리
        - [ ] [공통예외]이름 2자 이상인지 검사 👉 범위를 만족하지 못하면 예외처리
        - [ ] [노선예외]노선 중복 검사 👉 노선이 존재한다면(=`노선이중복됨`) 예외 처리

    - [ ] 등록할 상행/하행 종점역 이름 입력 안내 출력

        - [ ] [공통예외]한글 이외의 입력값 검사 👉 한글이 아니라면 예외처리
        - [ ] [공통예외]이름 2자 이상인지 검사 👉 범위를 만족하지 못하면 예외처리
        - [ ] [역예외]역 중복 검사 👉 역이 존재하지않는다면(=`역이중복되지않음`) 예외처리

    - [ ] 노선 등록 : 완료 안내 출력

2. `3-c` 노선 삭제

    - [ ] 삭제할 노선 입력 안내

        - [ ] [공통예외]한글 이외의 입력값 검사 👉 한글이 아니라면 예외처리
        - [ ] [공통예외]이름 2자 이상인지 검사 👉 범위를 만족하지 못하면 예외처리
        - [ ] [노선예외]노선 중복 검사 👉 노선이 존재하지 않는다면(=`노선이중복되지않음`) 예외처리

    - [ ] 노선 삭제 : 완료 안내 출력

3. `3-d` 노선 조회

    - [ ] 노선 목록 출력하기

4. `3-e` 돌아가기 (`B`)

<br>

### `기능4` 구간 관리<br>

`4-a`. 구간 관리 기능이 제대로 수행되면 메인으로, 에러인 경우 노선 관리로 돌아간다.

1. `4-b` 구간 등록

    - [ ] 등록할 구간의 노선 입력 안내 출력
    - [ ] 등록할 구간의 노선 입력 받기

        - [ ] [공통예외]한글 이외의 입력값 검사 👉 한글이 아니라면 예외처리
        - [ ] [공통예외]이름 2자 이상인지 검사 👉 범위를 만족하지 못하면 예외처리
        - [ ] [노선예외]노선 중복 검사 👉 노선이 존재하지 않는다면(=`노선이중복되지않음`) 예외처리

    - [ ] 구간에 등록할 역 이름 입력 안내 출력
    - [ ] 구간에 등록할 역 입력 받기

        - [ ] [공통예외]한글 이외의 입력값 검사하기 👉 한글이 아니라면 예외처리
        - [ ] [공통예외]이름 2자 이상인지 검사 👉 범위를 만족하지 못하면 예외처리
        - [ ] [역예외]역 중복 검사 👉 중복되지않는다면(=`역이존재하지않음`) 예외 처리
        - [ ] [노선예외2]노선에 역 중복 검사 👉 노선에 역이 중복된다면(=`노선에역이존재함`) 예외처리

    - [ ] 등록할 구간 순서 입력 안내 출력
    - [ ] 등록할 구간 순서 입력 받기

        - [ ] [구간예외]순서가 숫자 이외의 입력값 검사 👉 숫자가 아니라면 예외처리
        - [ ] [구간예외]`1 <= 순서 < 노선길이` 검사 👉 범위를 만족하지 못하면 예외처리

    - [ ] 구간 등록 : 완료 안내 출력

2. `4-c` 구간 삭제

    - [ ] 삭제할 구간의 노선 입력

        - [ ] [공통예외]한글 이외의 입력값 검사 👉 한글이 아니라면 예외처리
        - [ ] [공통예외]이름 2자 이상인지 검사 👉 범위를 만족하지 못하면 예외처리
        - [ ] [노선예외]노선 중복 검사 👉 노선이 존재하지 않는다면(=`노선이중복되지않음`) 예외처리
        - [ ] 노선의 길이가 2 이하라면 예외처리

    - [ ] 삭제할 구간의 역 입력

        - [ ] [공통예외]한글 이외의 입력값 검사 👉 한글이 아니라면 예외처리
        - [ ] [공통예외]이름 2자 이상인지 검사 👉 범위를 만족하지 못하면 예외처리
        - [ ] [역예외]역 중복 검사 👉 중복되지 않는다면(=`역이존재하지않음`) 예외 처리
        - [ ] [노선예외2]노선에 역 중복 검사 👉 노선에 역이 중복되지않는다면(=`노선에역이존재하지않음`) 예외처리

    - [ ] 구간 삭제 : 완료 안내 출력

        - [ ] 구간의 종점역을 삭제하는 경우 종점을 재정의

3. `4-d` 돌아가기 (`B`)

<br>

### `기능5` 지하철 노선도 출력<br>

`5-a`. 지하철 노선 출력
`5-b`. 지하철 노선별 역 출력

<br>

### `기능6` 종료<br>

`6-a`. `Q`를 누르면 메인 프로그램 종료

<br>

# 프로그램 요구사항

### 프로그래밍 요구사항 - Station, Line
- [ ] Station, Line 클래스를 활용하여 지하철역과 노선을 구현해야 한다.
- [ ] 제공하는 각 클래스의 기본 생성자를 추가할 수 없다.
- [ ] 필드(인스턴스 변수)인 name의 접근 제어자 private을 변경할 수 없다.
- [ ] 가능하면 setter 메소드(ex. setXXX)를 추가하지 않고 구현한다.

<br>

### 프로그래밍 요구사항 - StationRepository, LineRepository
- [ ] Station과 Line의 상태를 저장할 수 있는 StationRepository, LineRepository를 제공한다.
- [ ] 필요 시 StationRepository, LineRepository 이 외 추가로 Repository를 만들 수 있다.
- [ ] 추가로 생성되는 객체에 대해서 XXXRepository 네이밍으로 저장 클래스를 추가할 수 있다.
- [ ] 객체들의 상태를 관리하기 위해서 XXXRepository 클래스를 활용해 저장 로직을 구현해야 한다.

<br>

### 피드백 요구사항

- [ ] 값을 하드코딩하지 마라 :: 상수는 항상 체크하자
- [ ] 배열 대신 java collection을 사용하라 :: List, Set, Map 등
- [ ] 필드(인스턴스 변수)의 수를 줄이기 위해 노력한다
- [ ] 비지니스 로직과 UI 로직을 분리해라.

    - [ ] 현재 객체의 상태를 보기 위한 로그 메시지 성격이 강하다면 toString()을 통해 구현한다.
    - [ ] View에서 사용할 데이터라면 getter 메소드를 통해 데이터를 전달한다.

- [ ] 함수는 15라인 이하로 구성한다. (Main도 마찬가지)
- [ ] 객체에 메시지를 보내라 :: 상태 데이터를 가지는 객체에서 데이터를 꺼내려(get)하지 말고 객체에 메시지를 보내라.

