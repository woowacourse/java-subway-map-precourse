### 초기 설정

- 프로그램 시작 시 역, 노선 등 필요한 정보를 미리 셋팅할 수 있다.

> 아래의 사전 등록 정보로 반드시 초기 설정을 하기
>

```
 1. 지하철역으로 교대역, 강남역, 역삼역, 남부터미널역, 양재역, 양재시민의숲역, 매봉역이 등록되어 있다.
 2. 지하철 노선으로 2호선, 3호선, 신분당선이 등록되어 있다.
 3. 노선에 역이 아래와 같이 등록되어 있다.(왼쪽 끝이 상행 종점)
   - 2호선: 교대역 - 강남역 - 역삼역
   - 3호선: 교대역 - 남부터미널역 - 양재역 - 매봉역
   - 신분당선: 강남역 - 양재역 - 양재시민의숲역
 ```

<img src="image/domain.png" width="500">

### 지하철 역 관련 기능

- 지하철 역을 등록하고 삭제할 수 있다. (단, 노선에 등록된 역은 삭제할 수 없다)
- 중복된 지하철 역 이름이 등록될 수 없다.
- 지하철 역 이름은 2글자 이상이어야 한다.
- 지하철 역의 목록을 조회할 수 있다.

### 지하철 노선 관련 기능

- 지하철 노선을 등록하고 삭제할 수 있다.
- 중복된 지하철 노선 이름이 등록될 수 없다.
- 지하철 노선 이름은 2글자 이상이어야 한다.
- 노선 등록 시 상행 종점역과 하행 종점역을 입력받는다.
- 지하철 노선의 목록을 조회할 수 있다.

### 지하철 구간 추가 기능

- 지하철 노선에 구간을 추가하는 기능은 노선에 역을 추가하는 기능이라고도 할 수 있다.
    - 역과 역사이를 구간이라 하고 이 구간들의 모음이 노선이다.
- 하나의 역은 여러개의 노선에 추가될 수 있다.
- 역과 역 사이에 새로운 역이 추가 될 수 있다.
- 노선에서 갈래길은 생길 수 없다.

<img src="image/section1.png" width="500">

### 지하철 구간 삭제 기능

- 노선에 등록된 역을 제거할 수 있다.
- 종점을 제거할 경우 다음 역이 종점이 된다.
- 노선에 포함된 역이 두개 이하일 때는 역을 제거할 수 없다.

<img src="image/section2.png" width="500">

### 지하철 노선에 등록된 역 조회 기능

- 노선의 상행 종점부터 하행 종점까지 연결된 순서대로 역 목록을 조회할 수 있다.

## ✍🏻 입출력 요구사항

- `프로그래밍 실행 결과 예시`를 참고하여 입출력을 구현한다.
- 기대하는 출력 결과는 `[INFO]`를 붙여서 출력한다. 출력값의 형식은 예시와 동일하게 한다.
- 에러 발생 시 `[ERROR]`를 붙여서 출력한다. (에러의 문구는 자유롭게 작성한다.)

- 메인화면에서 입력받는 기능

## 메인 화면

1. 역 관리
2. 노선 관리
3. 구간 관리
4. 지하철 노선도 출력
   Q. 종료

## 원하는 기능을 선택하세요.

- 역 관리에서 입력받는 기능

## 역 관리 화면

1. 역 등록
2. 역 삭제
3. 역 조회
   B. 돌아가기

## 원하는 기능을 선택하세요.

- 역 등록에서 역 이름 입력받는 기능

## 등록할 역 이름을 입력하세요.

- 역 이름 등록 후 메세지 출력
  [INFO] 지하철 역이 등록되었습니다.

- 역 관리 -> 역 조회시 저장된 역 출력

## 역 목록

[INFO] 교대역
[INFO] 강남역
[INFO] 역삼역
[INFO] 남부터미널역
[INFO] 양재역
[INFO] 양재시민의숲역
[INFO] 매봉역
[INFO] 잠실역

- 역 관리 -> 역 삭제시 삭제할 역 입력받는 기능

## 삭제할 역 이름을 입력하세요.

잠실역

- 역 삭제 후 메세지 출력
  [INFO] 지하철 역이 삭제되었습니다.

## 기능 목록

- 기본으로 제공된 역, 노선, 구간 정보를 초기화한다.
- 메인화면에서 기능 선택을 입력받는다.
    - [x] "## 원하는 기능을 선택하세요." 출력하고 기능을 입력받는다.
    - [x] 입력값이 1,2,3,4,Q 중에서 없으면 에러를 반환하고 재입력 받는다.
    - [ ] 1.역 관리
        - [ ] 1.역 등록
            - [x] "## 등록할 역 이름을 입력하세요." 출력하고 등록할 역 이름을 입력받는다.
            - [x] 중복된 지하철 역을 등록할 수 없다.
            - [x] 역 이름은 2글자 이상이어야 한다.
        - [ ] 2.역 삭제
            - [ ] 존재하지 않는 역을 삭제할 수 없다.
            - [ ] 노선에 등록된 역은 삭제할 수 없다.
        - [ ] 3.역 조회
            - [ ] 역 저장소에 저장된 모든 역을 출력한다.
        - [ ] B. 돌아가기
            - [ ] 메인메뉴 선택으로 돌아간다.
    - 지하철 노선을 등록하고 삭제할 수 있다.
      중복된 지하철 노선 이름이 등록될 수 없다.
      지하철 노선 이름은 2글자 이상이어야 한다.
      노선 등록 시 상행 종점역과 하행 종점역을 입력받는다.
      지하철 노선의 목록을 조회할 수 있다.
    - [ ] 2.노선 관리
        - [ ] "## 원하는 기능을 선택하세요." 출력하고 노선 관리 기능을 입력받는다.
        - [ ] 1,2,3,B 중에 존재하는지 검증한다.
        - [ ] 1.노선 등록
            - [ ] "## 등록할 노선 이름을 입력하세요." 출력하고 입력받는다.
            - [ ] 입력한 노선이 존재하는지 확인한다.
            - [ ] 존재한다면 에러를 반환하고 다시 입력받는다.
            - [ ] "## 등록할 노선의 상행 종점역 이름을 입력하세요." 출력하고 등록할 노선의 상행 종점역 이름을 입력받는다.
            - [ ] 입력한 상행 종점역이 역 저장소에 존재하는지 검증한다.
            - [ ] "## 등록할 노선의 하행 종점역 이름을 입력하세요." 출력하고 등록할 노선의 하행 종점역 이름을 입력받는다.
            - [ ] 입력한 하행 종점역이 역 저장소에 존재하는지 검증한다.
            - [ ] "[INFO] 지하철 노선이 등록되었습니다." 메세지를 출력한다.
        - [ ] 2.노선 삭제
            - [ ] "## 삭제할 노선 이름을 입력하세요." 출력하고 입력받는다.
            - [ ] 입력받은 노선이 존재하는지 검증한다.
            - [ ] "[INFO] 지하철 노선이 삭제되었습니다." 출력한다.
        - [ ] 3.노선 조회
            - [ ] "## 노선 목록" 출력한다.
            - [ ] "[INFO] %d호선" 형식으로 저장된 모든 노선 이름을 출력한다.
        - [ ] B. 돌아가기
            - [ ] 메인메뉴 선택으로 돌아간다.
    - [ ] 3.구간 관리
        - [ ] "## 원하는 기능을 선택하세요." 출력하고 구간 관리 기능을 입력받는다.
        - [ ] 1,2,B 중에 존재하는지 검증한다.
        - [ ] 1.구간 등록
            - [ ] "## 노선을 입력하세요." 출력하고 입력받는다.
            - [ ] 입력받은 노선이 존재하는지 검증한다.
            - [ ] "## 역이름을 입력하세요." 출력하고 구간 등록할 역 이름을 입력받는다.
            - [ ] 입력받은 역이 존재하는지 검증한다.
            - [ ] "## 순서를 입력하세요." 출력하고 순서를 입력받는다.
            - [ ] 해당 노선에 존재하는 역 개수에 부합하는지 검증한다.
            - [ ] 해당 노선에 입력받은 순서에 역을 등록한다.
            - [ ] "[INFO] 구간이 등록되었습니다." 출력한다.
        - [ ] 2.구간 삭제
            - [ ] "## 삭제할 구간의 노선을 입력하세요." 출력하고 노선을 입력받는다.
            - [ ] 입력받은 노선이 존재하는지 검증한다.
            - [ ] "## 삭제할 구간의 역을 입력하세요." 출력하고 역을 입력받는다.
            - [ ] 입력받은 역이 해당 노선에 존재하는지 검증한다.
            - [ ] 해당 노선의 역을 삭제한다.
        - [ ] B. 돌아가기
            - [ ] 메인메뉴 선택으로 돌아간다.
    - [ ] 4.지하철 노선도 출력
        - [ ] 전체 노선에 저장된 역을 출력한다.
            - [INFO] 2호선 형식으로 출력한다.
              [INFO] ---
              [INFO] 교대역
              [INFO] 강남역
              [INFO] 역삼역 ...

    - [ ] Q.종료
        - [ ] 프로그램을 종료한다.
